//import Pokedex from 'pokedex-promise-v2';
// RIP unable to import library 
const options = {
    versionPath: '/api/v2/',
    cacheLimit: 1000 * 1000, 
    timeout: 5 * 1000 
}

//export default P = new Pokedex(options);

